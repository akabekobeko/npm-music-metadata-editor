# Security Review — `@akabeko/music-metadata-editor-cli` v1.1.1

## メタ情報

| 項目 | 値 |
| --- | --- |
| 対象パッケージ | `@akabeko/music-metadata-editor-cli` (`packages/cli/`) |
| 対象バージョン | **v1.1.1** |
| 実施日 | 2026-09-02 |
| 実施者 | Claude Code |
| 実施方法 | `/security-review` Skill (脆弱性探索サブタスク → 誤検出フィルタ)。Finding 0 件のため誤検出フィルタ工程は対象なし。前回実施分 (PR #71) は本ファイルの git 履歴を参照 |
| ブランチ | `chore/update-env-pnpm12` |
| 対象 PR | `chore(multiple): update mise tools and dependencies (pnpm 12)` |
| 観点 | mise の pnpm backend 切り替え (`npm:pnpm` 11.21.0 → aqua `pnpm` 12.2.1)、Node 24.19.0 → 24.20.0、devDependencies の minor / patch 更新、pnpm 12 による lockfile 再生成がサプライチェーン / ビルド経路に与える影響 |

## サマリー

致命的な脆弱性は **検出されず**。本 PR は **実装コードへの変更なし** (cli の差分は `package.json` の devDependencies 4 件の range 更新のみ) で、フラグ処理 / 出力経路は v1.1.1 リリース時点から不変。ランタイム `dependencies` (`@akabeko/music-metadata-editor` `workspace:^` / `commander` ^15.0.0) も不変。

lockfile / ツール解決経路のサプライチェーン検査結果は [`../../core/security-review/v1.1.1.md`](../../core/security-review/v1.1.1.md) と共通: 追加 resolution 95 件すべてに sha512 integrity あり、非 registry 参照 0 件、lifecycle script を持つパッケージは `allowBuilds` 登録済みの 3 件のみ、`pnpm audit` で既知脆弱性なし。

|  | 件数 (差分起因) | 件数 (継続) |
| --- | --- | --- |
| Critical | 0 | 0 |
| High | 0 | 0 |
| Medium | 0 | 0 |
| Low / Informational | 0 | 既存レビューの継続項目 (据え置き) |

## 良い点 (Strengths)

- **実装コード非変更** — cli の差分は `@tsconfig/node24` / `@types/node` / `tsx` / `vitest` の devDependencies range 更新のみ。`commander` のバージョンは不変で、bin shim (`dist/bin/mme.js`) の生成経路 (`scripts/postBuildShebang.ts`) にも変更なし。
- **lockfile の来歴が健全** — core のレポートと同じ検査を通過 (registry / integrity / install script)。
- **検証済** — `pnpm install --frozen-lockfile` / `pnpm typecheck` / `pnpm check` / `pnpm build` / `node packages/cli/dist/bin/mme.js --version` / `pnpm test` (cli 248 件) が全通過。

## 所見 (Findings)

**該当なし**。

## 次回更新時のチェックリスト

- [ ] v1.0.3 チェックリストの継続項目 (`STRING_TAG_FIELDS` 追加時の構造メタ文字確認ほか) を再評価
- [ ] `commander` の major 更新時は、引数パースの挙動変化 (unknown option の扱い、`--` 以降のパス解釈) が入力検証に影響しないか確認
- [ ] lockfile 更新を伴う PR では、追加エントリーのレジストリ ホスト / integrity / install script の有無を再検査

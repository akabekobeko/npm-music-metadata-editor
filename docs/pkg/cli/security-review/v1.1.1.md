# Security Review — `@akabeko/music-metadata-editor-cli` v1.1.1

## メタ情報

| 項目 | 値 |
| --- | --- |
| 対象パッケージ | `@akabeko/music-metadata-editor-cli` (`packages/cli/`) |
| 対象バージョン | **v1.1.1** |
| 実施日 | 2026-08-16 |
| 実施者 | Claude Code |
| 実施方法 | 差分が `package.json` の `version` フィールド 1 行のみのため、`/security-review` Skill の観点を手動レビューでカバー |
| ブランチ | `chore/bump-patch-versions` |
| 対象 PR | `chore(multiple): bump patch versions for genre fixes (v1.1.1)` |
| 観点 | patch バージョン引き上げ (1.1.0 → 1.1.1) に伴う差分の健全性 |

## サマリー

致命的な脆弱性は **検出されず**。本バージョンの差分は **`version` の引き上げのみ** (1.1.0 → 1.1.1) で、CLI の実装コード・依存関係への変更は無い。本体ライブラリへの依存は `workspace:^` のまま不変で、lockfile にも差分なし。

v1.1.1 で CLI の挙動に影響する変更は core 側の修正 (ID3v2 TCON 数値参照の解決など、PR #67〜#70) のみで、それらのレビューは [`../../core/security-review/v1.1.1.md`](../../core/security-review/v1.1.1.md) から辿れる各実施記録を参照: いずれも新規 Finding 0 件。

|  | 件数 (差分起因) | 件数 (継続) |
| --- | --- | --- |
| Critical | 0 | 0 |
| High | 0 | 0 |
| Medium | 0 | 0 |
| Low / Informational | 0 | 既存レビューの継続項目 (据え置き) |

## 良い点 (Strengths)

- **実装コード非変更** — 差分は `package.json` の `version` のみ。フラグ処理 / 出力経路は v1.1.0 から不変。
- **検証済** — `pnpm check` / `pnpm typecheck` / `pnpm test` (cli 248 件) が全通過。

## 所見 (Findings)

**該当なし**。

## 次回更新時のチェックリスト

- [ ] v1.0.3 チェックリストの継続項目 (`STRING_TAG_FIELDS` 追加時の構造メタ文字確認ほか) を再評価
- [ ] core の `tagSources` / `ID3V1_GENRES` を CLI から利用し始める際は、出力面 (JSON / pretty) への露出内容を確認
- [ ] `dependencies` にランタイム依存を追加した場合は、当該パッケージの既知 CVE を確認

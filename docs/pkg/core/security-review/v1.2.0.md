# Security Review — `@akabeko/music-metadata-editor` v1.2.0

## メタ情報

| 項目 | 値 |
| --- | --- |
| 対象パッケージ | `@akabeko/music-metadata-editor` (`packages/core/`) |
| 対象バージョン | **v1.2.0** |
| 実施日 | 2026-09-23 |
| 実施者 | Claude Code |
| 実施方法 | 差分が `package.json` の `version` フィールド 1 行のみのため、`/security-review` Skill の観点を手動レビューでカバー。機能差分 (`null` によるタグ フィールド削除 / rating 対応) のレビューは前 PR で実施済みの `v1.1.1.md` (PR #76 / PR #77 実施分。前者は git 履歴) を参照 |
| ブランチ | `chore/bump-minor-versions` |
| 対象 PR | `chore(multiple): bump minor versions for tag deletion and rating support (v1.2.0)` |
| 観点 | minor バージョン引き上げ (1.1.1 → 1.2.0) に伴う差分の健全性 |

## サマリー

致命的な脆弱性は **検出されず**。本バージョンの差分は **`version` の引き上げのみ** (1.1.1 → 1.2.0) で、実装コード・依存関係への変更は無い。ランタイム `dependencies` は引き続き存在せず、lockfile にも差分なし (`pnpm install --frozen-lockfile --offline` が "Lockfile is up to date" を報告)。

v1.2.0 として公開される機能差分のセキュリティ レビューは [`v1.1.1.md`](v1.1.1.md) に記録済み:

- PR #76 (`TagPatch` / `SavableTrack` による `null` 削除、MP4 tombstone、`fillNumberPairs`): Finding 0 件 (本ファイルの git 履歴)
- PR #77 (ID3v2 `POPM` / Vorbis Comment `RATING` / APE `Rating` の読み書き): Finding は防御的な INFO-2 (POPM e-mail の NUL 除去をエンコード後のバイト列で実施) 1 件で、同 PR 内で対処済

|  | 件数 (差分起因) | 件数 (継続) |
| --- | --- | --- |
| Critical | 0 | 0 |
| High | 0 | 0 |
| Medium | 0 | 0 |
| Low / Informational | 0 | v1.0.0 の LOW-1〜LOW-6 / v1.0.2〜v1.0.4 の INFO / INFO-1 (`ID3V1_GENRES` freeze 未実施) (据え置き) |

## 良い点 (Strengths)

- **実装コード非変更** — 差分は `package.json` の `version` のみ。パース / 書き込み経路は v1.1.1 + PR #76 / #77 から不変。
- **検証済** — `pnpm install --frozen-lockfile --offline` / `pnpm typecheck` / `pnpm check` / `pnpm build` / `pnpm test` (core 617 件) が全通過。

## 所見 (Findings)

**該当なし**。

## 次回更新時のチェックリスト

- [ ] v1.1.1 (PR #77 実施分) チェックリストの継続項目 (LOW-1〜LOW-6 / INFO-1 `ID3V1_GENRES` freeze / `applyTagPatch` の動的キー処理を外部入力へ広げる場合の `__proto__` ガード / MP4 tombstone の適用範囲 / `POPM` e-mail を公開 API にする場合の Latin-1 外文字の扱い / `RATING` 規約拡張時の星数判定) を再評価
- [ ] `dependencies` にランタイム依存を追加した場合は、当該パッケージの既知 CVE を `npm audit` / GitHub Advisory で確認
- [ ] lockfile 更新を伴う PR では、追加エントリーのレジストリ ホスト / integrity / install script の有無を再検査
